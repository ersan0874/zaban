# فاز ۴ — اتصال Flutter به API

## هدف
اپ دیگر از داده‌ی جعلی محلی تغذیه نشود؛ از سرور واقعی بخواند.

## کارها
- [x] پکیج `dio` + interceptor توکن/رفرش
- [x] Repository برای path / unit / lesson session
- [x] جایگزینی مسیر اصلی `SampleData`
- [x] حالت لودینگ، خطا، خالی
- [x] `exerciseCount` روی unit lessons در API

## DoD
- [x] Path از API می‌آید
- [x] Study/Quiz از session سرور تغذیه می‌شود
- [x] بدون شبکه پیام واضح نشان داده می‌شود
